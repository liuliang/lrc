<% form_for(@role, :url=>"#{ @is_new.blank? ? admin_role_path(@role) : admin_roles_path }") do |f| %>
  <%= f.label :name,'名字' %>
  <%= f.text_field :name %>
  <br/>
  <br/>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>
